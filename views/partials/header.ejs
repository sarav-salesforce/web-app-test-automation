<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | QA Test Playground</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles.css" />
    <script defer src="/app.js"></script>
  </head>
  <body class="<%= bodyClass || '' %>">
    <div class="app-shell">
      <header class="top-nav">
        <a class="brand" href="/">
          <div class="brand-mark">QA</div>
          <div class="brand-copy">
            <div class="brand-title">Test Playground</div>
            <div class="brand-subtitle">Quality Commerce Suite</div>
          </div>
        </a>
        <nav class="nav-links">
          <a href="/">Catalog</a>
          <a href="/orders">Orders</a>
          <a href="/cart" class="cart-link">
            Cart
            <span class="cart-count"><%= typeof cartCount !== 'undefined' ? cartCount : 0 %></span>
          </a>
        </nav>
      </header>
      <div class="toast-stack" data-toast-stack></div>
